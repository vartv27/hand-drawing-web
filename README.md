# 🎨 Hand Drawing Web App

Веб-приложение для рисования жестами руки с использованием камеры и AI распознавания жестов.

## ✨ Возможности

- 📹 Отслеживание руки в реальном времени через веб-камеру
- ✍️ Рисование при сближении указательного и большого пальцев
- 🎨 Выбор цвета и толщины линии
- 🗑️ Очистка холста
- 🖐️ Интуитивное управление жестами
- 🎵 **Музыкальный синтезатор** - играйте музыку жестами руки!
- 🎹 **20+ инструментов** - флейта, скрипка, гитара, пианино, терменвокс и др.
- 🎼 **Проигрывание мелодий** - встроенные мелодии и гамма
- 🔊 Управление высотой тона (влево-вправо) и громкостью (вверх-вниз)

## 🚀 Быстрый старт

### Запуск локально

1. Откройте `index.html` в современном браузере (Chrome, Edge, Firefox)
2. Разрешите доступ к камере
3. Покажите руку камере
4. Соедините указательный и большой пальцы для рисования
5. Разведите пальцы, чтобы перестать рисовать

### Через локальный сервер (рекомендуется)

```bash
# Python 3
python3 -m http.server 8000

# Node.js (npx)
npx http-server -p 8000

# Go
go run -m http.server 8000
```

Откройте браузер: `http://localhost:8000`

## 🎮 Управление

### Жесты руки:
- **Рисование**: Сомкните указательный и большой пальцы (щипок)
- **Перемещение**: Разведите пальцы и двигайте руку

### Кнопки:
- **Цвет**: Выберите цвет для рисования
- **Толщина**: Настройте толщину линии (1-20 px)
- **Очистить**: Очистить весь холст

## 🛠️ Технологии

- **HTML5 Canvas** - Рисование
- **MediaPipe Hands** - AI распознавание руки
- **JavaScript** - Логика приложения
- **CSS3** - Стилизация

## 📋 Требования

- Современный браузер с поддержкой:
  - WebRTC (доступ к камере)
  - HTML5 Canvas
  - ES6 JavaScript
- Веб-камера
- HTTPS или localhost (для доступа к камере)

## 🔧 Как это работает

1. **MediaPipe Hands** отслеживает 21 ключевую точку руки
2. Вычисляется расстояние между кончиком большого (точка 4) и указательного (точка 8) пальцев
3. Если расстояние < 30 пикселей → режим рисования активен
4. Позиция указательного пальца используется как координаты кисти
5. Линии рисуются на Canvas между последовательными позициями

## 📝 Структура проекта

```
hand-drawing-web/
├── index.html      # Основная HTML страница
├── style.css       # Стили
├── script.js       # Логика приложения
└── README.md       # Документация
```

## ⚠️ ВАЖНО: Требуется HTTPS!

**Приложение НЕ РАБОТАЕТ через HTTP на удаленных серверах!**

Современные браузеры требуют HTTPS для доступа к камере по соображениям безопасности. HTTP работает **только на localhost** для разработки.

### Как настроить HTTPS:

#### Вариант 1: Let's Encrypt (бесплатный SSL)

**Для Apache:**
```bash
sudo apt-get install certbot python3-certbot-apache
sudo certbot --apache -d romam.ru
```

**Для Nginx:**
```bash
sudo apt-get install certbot python3-certbot-nginx
sudo certbot --nginx -d romam.ru
```

#### Вариант 2: Cloudflare (проще всего)

1. Зарегистрируйтесь на https://www.cloudflare.com
2. Добавьте ваш домен (romam.ru)
3. Измените DNS на серверы Cloudflare
4. В настройках SSL/TLS включите "Always Use HTTPS"
5. Готово! Cloudflare автоматически даст HTTPS

#### Вариант 3: GitHub Pages (бесплатный хостинг)

1. Создайте репозиторий на GitHub
2. Загрузите файлы проекта
3. Включите GitHub Pages в настройках
4. Сайт доступен: `https://username.github.io/hand-drawing-web`

### Сообщения об ошибках:

- `No navigator.mediaDevices.getUserMedia exists` - используется HTTP вместо HTTPS
- `Камера недоступна. Требуется HTTPS` - нужно настроить SSL сертификат
- `Требуется HTTPS! Камера не работает через HTTP` - откройте сайт через https://

## 🐛 Известные проблемы

- Требуется хорошее освещение для точного отслеживания руки
- **Не работает на HTTP** (кроме localhost) - нужен HTTPS
- На мобильных устройствах обязателен HTTPS

## 📄 Лицензия

MIT

## 🤝 Автор

Создано с помощью Claude Code
